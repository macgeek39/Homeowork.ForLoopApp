

/*
 * Ask the user for a comma-separated list
 * of first names (no spaces). Split the 
 * string into a string array. 
 * Loop through the array and print
 * "Hello <name>" to the console for 
 * each person.
 */

// Ask the user for a comm-separated list of first names
// Split the string into a string array
// loop through the array
// print "Hello <name>" to the console for each person